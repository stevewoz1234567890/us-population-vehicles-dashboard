services:
  api:
    build: ./server
    ports:
      - "8001:8000"
    restart: unless-stopped

  web:
    build: ./client
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped
